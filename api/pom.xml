<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.openmrs.module</groupId>
		<artifactId>chartsearchai</artifactId>
		<version>1.0.0-SNAPSHOT</version>
	</parent>

	<artifactId>chartsearchai-api</artifactId>
	<packaging>jar</packaging>
	<name>Chart Search AI Module - API</name>
	<description>API project for the Chart Search AI Module</description>

	<dependencies>
		<dependency>
			<groupId>org.openmrs.api</groupId>
			<artifactId>openmrs-api</artifactId>
			<type>jar</type>
		</dependency>
		<dependency>
			<groupId>org.openmrs.module</groupId>
			<artifactId>llama-server-natives</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>org.openmrs.module</groupId>
			<artifactId>querystore-api</artifactId>
			<version>1.0.0-SNAPSHOT</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.openmrs.api</groupId>
			<artifactId>openmrs-api</artifactId>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.openmrs.test</groupId>
			<artifactId>openmrs-test</artifactId>
			<type>pom</type>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
		</resources>
		<!-- One filtered stanza, because every test resource is text. The second, unfiltered stanza
		     that used to sit here existed only to keep src/test/resources/embedding-cache/**'s 794
		     binary vectors out of Maven's resource filtering; those were deleted in #204 (nothing had
		     read them since the in-process embedding stack was removed in #51). Filtering stays on
		     for the same behaviour as before — restore an unfiltered stanza if a BINARY test resource
		     is ever added, since filtering corrupts one. -->
		<testResources>
			<testResource>
				<directory>src/test/resources</directory>
				<filtering>true</filtering>
			</testResource>
		</testResources>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<executions>
					<!--
						Publishes this module's test classes as a `tests`-classified artifact, so omod's own
						tests can use LogCapture — the one instrument in this repo that asserts the LEVEL an
						outcome is reported at, which several api tests already depend on and which an omod
						copy would duplicate at 400 lines of accumulated special cases (issue #450).

						omod's dependency on it is test-scoped, and omod's unpack-dependencies execution
						excludes this classifier explicitly — see the comment there. Without that exclusion
						these test classes would land in the shipped .omod.
					-->
					<execution>
						<id>Publish test classes for omod tests</id>
						<goals>
							<goal>test-jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
